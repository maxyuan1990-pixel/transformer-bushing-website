---
const { eyebrow, title, text, image = "/assets/hv-bushing-transformer-yard.jpg" } = Astro.props;
---

<section class="page-hero">
  <img src={image} alt="" />
  <div>
    <p class="eyebrow">{eyebrow}</p>
    <h1>{title}</h1>
    <p>{text}</p>
  </div>
</section>
